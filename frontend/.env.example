# These are container runtime variables, not build-time VITE_ vars.
# Pass via docker run -e or K8s deployment manifest.

BACKEND_HOST=localhost        # local: host.docker.internal | K8s: wms-backend-service
BACKEND_PORT=8000